<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gridview2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.EnroledActivity"></activity>
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.DetailsCourses" />
        <activity android:name=".Activities.CoursesDetails" />
        <activity android:name=".Activities.Mainactivities" />
        <activity android:name=".Activities.RegistrationActivity" />
        <activity android:name=".Activities.Login" />
        <activity android:name=".Activities.Launcher" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>